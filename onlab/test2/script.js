$(function () {
    // const $tr = $('tr');

    // for (let i = 0; i < $tr.length; i++) {
    //     const tr = $tr[i];
    //     const $td = $(tr).children().eq(i);
    //     $td.css({ backgroundColor: 'red' });
    // }

    // for (let i = 0; i < $tr.length; i++) {
    //     const tr = $tr[i];
    //     const $td = $(tr).children().filter(`:nth-child(${i + 1}), :nth-child(${$tr.length - i})`);
    //     $td.css({ backgroundColor: "red" });
    // }

    // $('tr').each(function (index, tr) {
    //     const $tr = $("tr");
    //     $(tr)
    //       .children()
    //         .filter(`:nth-child(${index + 1}), :nth-child(${$tr.length - index})`)
    //         .css({ backgroundColor: "red" });
    // })
    // const $box = $('.box');
    // console.log($box.html());
    // $box.html('<p>LOL</p>');
    // $box.text('hihi');
    // $box.attr({ class: 'box1 box2 box3' });
    // console.log($box.width());

    // const $input = $('.text')
    // console.log($input.val());
    // $input.val('123')
    });


